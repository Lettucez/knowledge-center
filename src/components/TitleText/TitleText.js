import React from 'react';

const TitleText = (props) => {

    return (
        <>
            <h2 className="title" style={{textAlign: "center", color: "#122956"}}>Have a Question?</h2>
            <p style={{textAlign: "center"}}>Please select a category below to show all related items for that category or begin typing to search for a particular item.</p>
        </>
    );
};

export default TitleText;